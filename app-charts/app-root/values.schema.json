{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "required": ["global"],
  "properties": {
    "envConfig": {
      "type": "object",
      "required": ["riverEnv"],
      "properties": {
        "env": {
          "type": "string",
          "enum": ["alpha", "gamma", "omega", "delta"]
        }
      }
    }
  }
}
