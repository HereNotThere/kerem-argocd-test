apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-root.fullname" . }}-test-connection"
  labels:
    {{- include "app-root.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-root.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
