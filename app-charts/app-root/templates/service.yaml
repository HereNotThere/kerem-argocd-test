apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-root.fullname" . }}
  labels:
    {{- include "app-root.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-root.selectorLabels" . | nindent 4 }}
