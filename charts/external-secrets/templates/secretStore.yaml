# apiVersion: v1
# kind: ServiceAccount
# metadata:
#   # TODO: find a way to refer to the environment name (if terraform creates the operator service account using the environment name)
#   name: external-secrets 
#   namespace: {{ .Release.Namespace }}
#   annotations:
#     iam.gke.io/gcp-service-account: external-secrets-operator@{{ .Values.global.gcp.projectId }}.iam.gserviceaccount.com
# ---
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: gcp-secret-store
  namespace: {{ .Release.Namespace }}
spec:
  provider:
    gcpsm:
      # projectId: {{ .Values.global.gcp.projectId }} # TODO: is this necessary?
      auth:
        workloadIdentity: 
          clusterLocation: {{ .Values.global.gcp.gke.clusterLocation }}
          clusterName: {{ .Values.global.gcp.gke.clusterName }}
          clusterProjectID: {{ .Values.global.gcp.projectId }}
          serviceAccountRef:
            name: external-secrets 

# ---
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: my-own-secret
#   namespace: {{ .Release.Namespace }}
# spec:
#   refreshInterval: "1h"
#   secretStoreRef:
#     name: gcp-secret-store
#     kind: SecretStore
#   target:
#     # name: synced-secret # optional, defaults to metadata.name
#     creationPolicy: Owner
#     deletionPolicy: Retain
#   data:
#   - secretKey: username
#     remoteRef:
#       key: database-credentials
#       version: v1
#       property: username